#!/usr/bin/env node

var bitcore = require('bitcore-node-safe/lib/cli/bitcore');
var path = require('path');
var servicesPath = path.resolve(__dirname, '../');
var additionalServices = ['insight-api-safe', 'insight-ui-safe'];
bitcore(servicesPath, additionalServices);
